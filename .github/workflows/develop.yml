
name: My first Workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome
              run: echo "My first github actions job"
            - name: List files
              run: ls
            - name: Readfile
              run: cat .README.md
            - name: script
              run: ./hello.sh